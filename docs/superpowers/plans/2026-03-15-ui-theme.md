# UI Theme personnalisable — Plan d'implémentation

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre à l'auteur de remplacer les 8 assets Kenney par ses propres images PNG, par story, avec application au mode Play et à l'export.

**Architecture:** Les assets custom sont stockés dans `stories/{nom}/assets/ui/` (git-tracké). `game_theme.gd` reçoit un `story_ui_path` optionnel et fait fallback sur Kenney si le fichier custom est absent. L'onglet "Thème UI" dans `menu_config_dialog.gd` gère l'import et la suppression des assets.

**Tech Stack:** GDScript 4, Godot 4.6.1, GUT 9.3.0 (tests)

**Spec:** `docs/superpowers/specs/2026-03-15-ui-theme-design.md`

**Commande de test :**
```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 120 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd
```

---

## Chunk 1 : Modèle de données + Thème dynamique + Bannière

---

### Task 1 : story.gd — champ `ui_theme_mode`

**Files:**
- Modify: `src/models/story.gd`
- Test: `specs/models/test_story.gd`

- [ ] **Étape 1 : Écrire les tests qui échouent**

Ajouter à la fin de `specs/models/test_story.gd` :

```gdscript
func test_ui_theme_mode_default_value() -> void:
	var story = Story.new()
	assert_eq(story.ui_theme_mode, "default", "ui_theme_mode should default to 'default'")

func test_to_dict_includes_ui_theme() -> void:
	var story = Story.new()
	story.ui_theme_mode = "custom"
	var d = story.to_dict()
	assert_true(d.has("ui_theme"), "to_dict should include 'ui_theme' key")
	assert_eq(d["ui_theme"]["mode"], "custom")

func test_to_dict_ui_theme_default() -> void:
	var story = Story.new()
	var d = story.to_dict()
	assert_eq(d["ui_theme"]["mode"], "default")

func test_from_dict_reads_ui_theme_mode() -> void:
	var d = {"ui_theme": {"mode": "custom"}}
	var story = Story.from_dict(d)
	assert_eq(story.ui_theme_mode, "custom")

func test_from_dict_missing_ui_theme_defaults_to_default() -> void:
	var d = {}  # ancienne story sans ui_theme
	var story = Story.from_dict(d)
	assert_eq(story.ui_theme_mode, "default", "Missing field should default to 'default'")
```

- [ ] **Étape 2 : Lancer le test pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/models/test_story.gd
```
Attendu : FAIL sur `test_ui_theme_mode_default_value` (champ inexistant)

- [ ] **Étape 3 : Implémenter dans `src/models/story.gd`**

3a. Après la ligne `var show_title_banner: bool = true` (ligne 35), ajouter :
```gdscript
var ui_theme_mode: String = "default"
```

3b. Dans `to_dict()`, dans le dict retourné (après `"show_title_banner": show_title_banner,`), ajouter :
```gdscript
"ui_theme": { "mode": ui_theme_mode },
```

3c. Dans `from_dict()`, après la ligne qui lit `show_title_banner` (chercher `show_title_banner`), ajouter :
```gdscript
story.ui_theme_mode = d.get("ui_theme", {}).get("mode", "default")
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/models/test_story.gd
```
Attendu : PASS sur tous les tests story

- [ ] **Étape 5 : Commit**

```bash
git add src/models/story.gd specs/models/test_story.gd
git commit -m "feat(story): add ui_theme_mode field with serialization"
```

---

### Task 2 : game_theme.gd — `_resolve_asset` + signatures avec `story_ui_path`

**Files:**
- Modify: `src/ui/themes/game_theme.gd`
- Test: `specs/ui/themes/test_game_theme.gd`

- [ ] **Étape 1 : Écrire les tests qui échouent**

Ajouter à la fin de `specs/ui/themes/test_game_theme.gd` :

```gdscript
# --- _resolve_asset ---

func test_resolve_asset_empty_path_returns_kenney_asset() -> void:
	# Sans chemin custom, doit retourner le Kenney ou null en headless
	var tex = GameTheme._resolve_asset("button_brown.png", "")
	# En headless, load peut retourner null — on teste juste que ça ne plante pas
	assert_true(tex == null or tex is Texture2D)

func test_resolve_asset_nonexistent_custom_falls_back_to_kenney() -> void:
	var tex = GameTheme._resolve_asset("button_brown.png", "/nonexistent/path/that/does/not/exist")
	assert_true(tex == null or tex is Texture2D)

func test_resolve_asset_existing_custom_file_is_preferred() -> void:
	# Créer un fichier PNG temporaire valide (1x1 pixel PNG)
	var tmp_dir = "/tmp/test_ui_theme_" + str(randi())
	DirAccess.make_dir_recursive_absolute(tmp_dir)
	# PNG 1x1 blanc minimal (valide)
	var img = Image.create(1, 1, false, Image.FORMAT_RGB8)
	img.save_png(tmp_dir + "/button_brown.png")

	var tex = GameTheme._resolve_asset("button_brown.png", tmp_dir)
	# En mode headless les textures peuvent ne pas charger, mais FileAccess doit détecter le fichier
	assert_true(tex == null or tex is Texture2D)

	# Cleanup
	DirAccess.remove_absolute(tmp_dir + "/button_brown.png")
	DirAccess.remove_absolute(tmp_dir)

func test_create_theme_with_empty_path_returns_theme() -> void:
	var theme = GameTheme.create_theme("")
	assert_not_null(theme)
	assert_true(theme is Theme)

func test_create_theme_with_nonexistent_path_returns_theme() -> void:
	var theme = GameTheme.create_theme("/nonexistent/path")
	assert_not_null(theme)
	assert_true(theme is Theme)

func test_apply_danger_style_accepts_story_ui_path() -> void:
	var btn = Button.new()
	add_child(btn)
	# Ne doit pas planter avec un chemin vide
	GameTheme.apply_danger_style(btn, "")
	remove_child(btn)
	btn.queue_free()

func test_apply_close_style_accepts_story_ui_path() -> void:
	var btn = Button.new()
	add_child(btn)
	GameTheme.apply_close_style(btn, "")
	remove_child(btn)
	btn.queue_free()

func test_apply_dark_panel_style_accepts_story_ui_path() -> void:
	var panel = PanelContainer.new()
	add_child(panel)
	GameTheme.apply_dark_panel_style(panel, "")
	remove_child(panel)
	panel.queue_free()
```

- [ ] **Étape 2 : Lancer les tests pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/themes/test_game_theme.gd
```
Attendu : FAIL sur les nouveaux tests (`_resolve_asset` et signatures avec `story_ui_path`)

- [ ] **Étape 3 : Implémenter dans `src/ui/themes/game_theme.gd`**

3a. Ajouter le helper **avant** `static func create_theme()` (après les constantes SCROLLBAR_MARGIN). Il est `static func` (pas `func`) car appelé depuis d'autres static funcs, et accessible depuis l'extérieur (ex. `main_menu.gd` l'appelle via `GameTheme._resolve_asset(...)`) :

```gdscript
static func _resolve_asset(filename: String, story_ui_path: String) -> Texture2D:
	if story_ui_path != "":
		var custom_path = story_ui_path + "/" + filename
		if FileAccess.file_exists(custom_path):
			return load(custom_path)
	return load(ASSETS_PATH + filename)
```

3b. Modifier la signature de `create_theme` et passer `story_ui_path` uniquement aux 4 fonctions privées qui chargent des PNG (`_setup_button`, `_setup_option_button`, `_setup_panel_container`, `_setup_check_button`). Les 5 autres (`_setup_label`, `_setup_rich_text_label`, `_setup_separator`, `_setup_slider`, `_setup_scrollbar`) n'utilisent pas de PNG — ne pas les modifier :

```gdscript
static func create_theme(story_ui_path: String = "") -> Theme:
	var theme = Theme.new()
	_setup_button(theme, story_ui_path)
	_setup_option_button(theme, story_ui_path)
	_setup_panel_container(theme, story_ui_path)
	_setup_label(theme)
	_setup_rich_text_label(theme)
	_setup_check_button(theme, story_ui_path)
	_setup_separator(theme)
	_setup_slider(theme)
	_setup_scrollbar(theme)
	return theme
```

Mettre à jour les signatures des 4 fonctions privées concernées (remplacer `load(ASSETS_PATH + "...")` par `_resolve_asset("...", story_ui_path)`) :
```gdscript
static func _setup_button(theme: Theme, story_ui_path: String = "") -> void:
	var tex = _resolve_asset("button_brown.png", story_ui_path)
	# ... if tex == null: return  — reste inchangé

static func _setup_option_button(theme: Theme, story_ui_path: String = "") -> void:
	var tex = _resolve_asset("button_brown.png", story_ui_path)
	# ... reste inchangé

static func _setup_panel_container(theme: Theme, story_ui_path: String = "") -> void:
	var tex = _resolve_asset("panel_brown.png", story_ui_path)
	# ... reste inchangé

static func _setup_check_button(theme: Theme, story_ui_path: String = "") -> void:
	var unchecked = _resolve_asset("checkbox_brown_empty.png", story_ui_path)
	var checked = _resolve_asset("checkbox_brown_checked.png", story_ui_path)
	# ... reste inchangé
```

3c. Modifier les fonctions publiques `apply_danger_style`, `apply_close_style`, `apply_dark_panel_style` :

```gdscript
static func apply_danger_style(button: Button, story_ui_path: String = "") -> void:
	var tex = _resolve_asset("button_red.png", story_ui_path)
	# ... reste inchangé (remplacer load(ASSETS_PATH + "button_red.png") par tex)

static func apply_close_style(button: Button, story_ui_path: String = "") -> void:
	var tex = _resolve_asset("button_red_close.png", story_ui_path)
	# ... reste inchangé

static func apply_dark_panel_style(panel: PanelContainer, story_ui_path: String = "") -> void:
	var tex = _resolve_asset("panel_brown_dark.png", story_ui_path)
	# ... reste inchangé
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/themes/test_game_theme.gd
```
Attendu : PASS sur tous les tests (anciens + nouveaux)

- [ ] **Étape 5 : Commit**

```bash
git add src/ui/themes/game_theme.gd specs/ui/themes/test_game_theme.gd
git commit -m "feat(game-theme): add _resolve_asset helper and story_ui_path param to public API"
```

---

### Task 3 : main_menu.gd — `_banner_texture_rect` + `update_banner()`

**Files:**
- Modify: `src/ui/menu/main_menu.gd`
- Test: `specs/ui/menu/test_main_menu.gd`

- [ ] **Étape 1 : Écrire le test qui échoue**

Ouvrir `specs/ui/menu/test_main_menu.gd` et ajouter à la fin :

```gdscript
func test_update_banner_does_not_crash_with_empty_path() -> void:
	# update_banner doit exister et ne pas planter avec un chemin vide
	assert_true(_menu.has_method("update_banner"), "main_menu should have update_banner method")
	_menu.update_banner("")

func test_update_banner_does_not_crash_with_nonexistent_path() -> void:
	_menu.update_banner("/nonexistent/path/that/does/not/exist")
	# Pas de crash = succès

func test_update_banner_updates_texture_when_custom_file_exists() -> void:
	# Créer un PNG 1×1 temporaire valide
	var tmp_dir = "/tmp/test_banner_" + str(randi())
	DirAccess.make_dir_recursive_absolute(tmp_dir)
	var img = Image.create(1, 1, false, Image.FORMAT_RGB8)
	img.save_png(tmp_dir + "/banner_hanging.png")

	# Appeler update_banner — ne doit pas planter
	# Note : en headless _banner_texture_rect est null (créé dans `if banner_tex:` de build_ui()
	# qui requiert un GPU pour charger la texture). update_banner() gère ce cas avec
	# `if tex and _banner_texture_rect:` — le test vérifie que la méthode existe et ne plante pas.
	_menu.update_banner(tmp_dir)

	# Cleanup
	DirAccess.remove_absolute(tmp_dir + "/banner_hanging.png")
	DirAccess.remove_absolute(tmp_dir)
```

- [ ] **Étape 2 : Lancer le test pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/menu/test_main_menu.gd
```
Attendu : FAIL sur `test_update_banner_does_not_crash_with_empty_path`

- [ ] **Étape 3 : Implémenter dans `src/ui/menu/main_menu.gd`**

3a. Dans la section des variables membres (après `var _banner_wrapper: CenterContainer`), ajouter :
```gdscript
var _banner_texture_rect: TextureRect = null
```

3b. Dans `build_ui()`, trouver la ligne `var banner = TextureRect.new()` (ligne ~84) et après la création du TextureRect, ajouter l'assignation :
```gdscript
_banner_texture_rect = banner
```
(La bannière par défaut continue d'être chargée normalement avec `load(GameTheme.ASSETS_PATH + "banner_hanging.png")`)

> **Note headless :** La création de `banner` est dans un bloc `if banner_tex:` où `banner_tex = load(...)`. En mode headless (GPU absent), `load()` peut retourner `null`, donc `_banner_texture_rect` reste `null`. C'est intentionnel : `update_banner()` gère ce cas via `if tex and _banner_texture_rect:`.

3c. Ajouter la méthode `update_banner` après `build_ui()` :
```gdscript
func update_banner(story_ui_path: String) -> void:
	var tex = GameTheme._resolve_asset("banner_hanging.png", story_ui_path)
	if tex and _banner_texture_rect:
		_banner_texture_rect.texture = tex
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/menu/test_main_menu.gd
```
Attendu : PASS sur tous les tests main_menu

- [ ] **Étape 5 : Commit**

```bash
git add src/ui/menu/main_menu.gd specs/ui/menu/test_main_menu.gd
git commit -m "feat(main-menu): add _banner_texture_rect ref and update_banner() method"
```

---

## Chunk 2 : Menus + Dialog + Signal

---

### Task 4 : Menus — `apply_custom_theme()`

**Files:**
- Modify: `src/ui/menu/pause_menu.gd`, `src/ui/menu/save_load_menu.gd`, `src/ui/menu/chapter_scene_menu.gd`
- Test: `specs/ui/menu/test_pause_menu.gd`, `specs/ui/menu/test_save_load_menu.gd`, `specs/ui/menu/test_chapter_scene_menu.gd`

- [ ] **Étape 1 : Écrire les tests qui échouent**

Dans `specs/ui/menu/test_pause_menu.gd`, ajouter à la fin :
```gdscript
func test_apply_custom_theme_method_exists() -> void:
	assert_true(_menu.has_method("apply_custom_theme"), "pause_menu should have apply_custom_theme")

func test_apply_custom_theme_does_not_crash() -> void:
	_menu.apply_custom_theme("")
	_menu.apply_custom_theme("/nonexistent/path")
```

Dans `specs/ui/menu/test_save_load_menu.gd`, ajouter à la fin :
```gdscript
func test_apply_custom_theme_method_exists() -> void:
	assert_true(_menu.has_method("apply_custom_theme"), "save_load_menu should have apply_custom_theme")

func test_apply_custom_theme_does_not_crash() -> void:
	_menu.apply_custom_theme("")
```

Dans `specs/ui/menu/test_chapter_scene_menu.gd`, ajouter à la fin :
```gdscript
func test_apply_custom_theme_method_exists() -> void:
	assert_true(_menu.has_method("apply_custom_theme"), "chapter_scene_menu should have apply_custom_theme")

func test_apply_custom_theme_does_not_crash() -> void:
	_menu.apply_custom_theme("")
```

- [ ] **Étape 2 : Lancer les tests pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 60 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/menu/test_pause_menu.gd
```

- [ ] **Étape 3a : Implémenter dans `src/ui/menu/pause_menu.gd`**

Ajouter après `build_ui()` (chercher la fin de la fonction) :
```gdscript
func apply_custom_theme(story_ui_path: String) -> void:
	if _quit_button:
		GameTheme.apply_danger_style(_quit_button, story_ui_path)
```

- [ ] **Étape 3b : Implémenter dans `src/ui/menu/save_load_menu.gd`**

Dans `save_load_menu.gd`, le bouton close est actuellement une **variable locale** `close_btn` dans `build_ui()`. Il faut le promouvoir en variable membre :

1. Après les déclarations membres existantes (après `var _confirm_overlay: Control`), ajouter :
```gdscript
var _close_btn: Button
```

2. Dans `build_ui()`, remplacer `var close_btn := Button.new()` (ligne ~62) par `_close_btn = Button.new()` et mettre à jour toutes les références `close_btn` dans le reste de `build_ui()` par `_close_btn`.

3. Ajouter la méthode :
```gdscript
func apply_custom_theme(story_ui_path: String) -> void:
	if _close_btn:
		GameTheme.apply_close_style(_close_btn, story_ui_path)
```
*(Les boutons danger créés dynamiquement dans les cartes de sauvegarde sont ignorés — seul le bouton close statique est concerné.)*

- [ ] **Étape 3c : Implémenter dans `src/ui/menu/chapter_scene_menu.gd`**

Même patron que `save_load_menu.gd` : le bouton close est une variable locale `close_btn` dans `build_ui()`.

1. Après les déclarations membres existantes (après `var _chapters_container: VBoxContainer`), ajouter :
```gdscript
var _close_btn: Button
```

2. Dans `build_ui()`, remplacer `var close_btn := Button.new()` (ligne ~51) par `_close_btn = Button.new()` et mettre à jour toutes les références `close_btn` dans le reste de `build_ui()` par `_close_btn`.

3. Ajouter la méthode :
```gdscript
func apply_custom_theme(story_ui_path: String) -> void:
	if _close_btn:
		GameTheme.apply_close_style(_close_btn, story_ui_path)
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 60 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/menu/test_pause_menu.gd
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/menu/test_save_load_menu.gd
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/menu/test_chapter_scene_menu.gd
```
Attendu : PASS sur tous

- [ ] **Étape 5 : Commit**

```bash
git add src/ui/menu/pause_menu.gd src/ui/menu/save_load_menu.gd src/ui/menu/chapter_scene_menu.gd \
  specs/ui/menu/test_pause_menu.gd specs/ui/menu/test_save_load_menu.gd specs/ui/menu/test_chapter_scene_menu.gd
git commit -m "feat(menus): add apply_custom_theme() to pause/save_load/chapter_scene menus"
```

---

### Task 5 : menu_config_dialog.gd — Onglet "Thème UI"

**Files:**
- Modify: `src/ui/dialogs/menu_config_dialog.gd`
- Test: `specs/ui/dialogs/test_menu_config_dialog.gd`

**Vue de l'onglet :**
```
[ Par défaut ]  [ Personnaliser ]   ← deux Button dans ButtonGroup

Mode Par défaut : Label "Thème Kenney Adventure (par défaut)"
Mode Personnaliser :
  - Label "Assets personnalisés (N / 8)"
  - ScrollContainer avec VBoxContainer (liste des assets)
    - Chaque ligne: HBoxContainer > [TextureRect 48×48] [Label nom] [Button ✕] [Button Remplacer]
  - Button "📂 Parcourir…" (multi-sélection)
```

- [ ] **Étape 1 : Écrire les tests qui échouent**

Ajouter à `specs/ui/dialogs/test_menu_config_dialog.gd` :

```gdscript
# --- Onglet Thème UI ---

func test_has_ui_theme_tab() -> void:
	var tabs = _dialog.get_node("TabContainer")
	var found = false
	for i in tabs.get_tab_count():
		if tabs.get_tab_title(i) == "Thème UI":
			found = true
			break
	assert_true(found, "TabContainer doit avoir un onglet 'Thème UI'")

func test_setup_fills_ui_theme_mode_default() -> void:
	var story = _make_story()
	story.ui_theme_mode = "default"
	_dialog.setup(story, "/tmp/test_story")
	assert_eq(_dialog.get_ui_theme_mode(), "default")

func test_setup_fills_ui_theme_mode_custom() -> void:
	var story = _make_story()
	story.ui_theme_mode = "custom"
	_dialog.setup(story, "/tmp/test_story")
	assert_eq(_dialog.get_ui_theme_mode(), "custom")
```

- [ ] **Étape 2 : Lancer les tests pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/dialogs/test_menu_config_dialog.gd
```
Attendu : FAIL sur les 3 nouveaux tests

- [ ] **Étape 3 : Implémenter l'onglet dans `src/ui/dialogs/menu_config_dialog.gd`**

3a. Ajouter les nouvelles variables membres après `var _show_title_banner_check: CheckButton` :
```gdscript
# UI Theme tab
var _ui_theme_default_btn: Button
var _ui_theme_custom_btn: Button
var _ui_theme_default_panel: VBoxContainer
var _ui_theme_custom_panel: VBoxContainer
var _ui_theme_assets_list: VBoxContainer
var _ui_theme_mode: String = "default"
```

3b. Définir la constante des assets reconnus après les `const` existants :
```gdscript
const UI_THEME_ASSETS = [
	"button_brown.png", "button_red.png", "button_red_close.png",
	"panel_brown.png", "panel_brown_dark.png", "banner_hanging.png",
	"checkbox_brown_empty.png", "checkbox_brown_checked.png"
]
```

3c. Dans `_init()`, après la création du dernier onglet ("À suivre"), créer l'onglet UI :

```gdscript
# ── Onglet Thème UI ──────────────────────────────────────────────────────────
var ui_theme_vbox = VBoxContainer.new()
ui_theme_vbox.name = "Thème UI"
ui_theme_vbox.add_theme_constant_override("separation", 8)

# Boutons radio (ButtonGroup)
var bg = ButtonGroup.new()
var mode_hbox = HBoxContainer.new()
mode_hbox.add_theme_constant_override("separation", 4)

_ui_theme_default_btn = Button.new()
_ui_theme_default_btn.name = "DefaultBtn"
_ui_theme_default_btn.text = "Par défaut"
_ui_theme_default_btn.toggle_mode = true
_ui_theme_default_btn.button_group = bg
_ui_theme_default_btn.button_pressed = true
mode_hbox.add_child(_ui_theme_default_btn)

_ui_theme_custom_btn = Button.new()
_ui_theme_custom_btn.name = "CustomBtn"
_ui_theme_custom_btn.text = "Personnaliser"
_ui_theme_custom_btn.toggle_mode = true
_ui_theme_custom_btn.button_group = bg
mode_hbox.add_child(_ui_theme_custom_btn)

ui_theme_vbox.add_child(mode_hbox)

# Panneau mode défaut
_ui_theme_default_panel = VBoxContainer.new()
_ui_theme_default_panel.name = "DefaultPanel"
var default_label = Label.new()
default_label.text = "Thème Kenney Adventure (par défaut)"
_ui_theme_default_panel.add_child(default_label)
var default_desc = Label.new()
default_desc.text = "Le jeu utilisera le thème brun aventure intégré."
default_desc.autowrap_mode = TextServer.AUTOWRAP_WORD
_ui_theme_default_panel.add_child(default_desc)
ui_theme_vbox.add_child(_ui_theme_default_panel)

# Panneau mode personnalisé
_ui_theme_custom_panel = VBoxContainer.new()
_ui_theme_custom_panel.name = "CustomPanel"
_ui_theme_custom_panel.visible = false
_ui_theme_custom_panel.add_theme_constant_override("separation", 4)

var assets_label = Label.new()
assets_label.name = "AssetsCountLabel"
assets_label.text = "Assets personnalisés (0 / 8)"
_ui_theme_custom_panel.add_child(assets_label)

var scroll = ScrollContainer.new()
scroll.custom_minimum_size = Vector2(0, 150)
scroll.size_flags_vertical = Control.SIZE_EXPAND_FILL
_ui_theme_assets_list = VBoxContainer.new()
_ui_theme_assets_list.name = "AssetsList"
_ui_theme_assets_list.size_flags_horizontal = Control.SIZE_EXPAND_FILL
scroll.add_child(_ui_theme_assets_list)
_ui_theme_custom_panel.add_child(scroll)

var browse_all_btn = Button.new()
browse_all_btn.name = "BrowseAllButton"
browse_all_btn.text = "📂 Parcourir…"
browse_all_btn.pressed.connect(_on_browse_ui_assets_pressed)
_ui_theme_custom_panel.add_child(browse_all_btn)

ui_theme_vbox.add_child(_ui_theme_custom_panel)
tabs.add_child(ui_theme_vbox)

# Connexions
_ui_theme_default_btn.toggled.connect(_on_ui_theme_mode_toggled.bind("default"))
_ui_theme_custom_btn.toggled.connect(_on_ui_theme_mode_toggled.bind("custom"))
```

3d. Ajouter la méthode getter publique :
```gdscript
func get_ui_theme_mode() -> String:
	return _ui_theme_mode
```

3e. Dans `setup()` (chercher la fonction `setup`), ajouter après l'initialisation des autres champs :
```gdscript
_ui_theme_mode = _story.ui_theme_mode if _story and _story.get("ui_theme_mode") != null else "default"
if _ui_theme_mode == "custom":
	_ui_theme_custom_btn.button_pressed = true
else:
	_ui_theme_default_btn.button_pressed = true
_ui_theme_default_panel.visible = (_ui_theme_mode == "default")
_ui_theme_custom_panel.visible = (_ui_theme_mode == "custom")
_refresh_ui_theme_assets_list()
```

3f. Ajouter les méthodes privées de l'onglet :

```gdscript
func _on_ui_theme_mode_toggled(pressed: bool, mode: String) -> void:
	if not pressed:
		return
	_ui_theme_mode = mode
	_ui_theme_default_panel.visible = (mode == "default")
	_ui_theme_custom_panel.visible = (mode == "custom")


func _refresh_ui_theme_assets_list() -> void:
	for child in _ui_theme_assets_list.get_children():
		child.queue_free()
	if _story_base_path == "":
		return
	var ui_dir = _story_base_path + "/assets/ui"
	var count = 0
	for filename in UI_THEME_ASSETS:
		var path = ui_dir + "/" + filename
		if FileAccess.file_exists(path):
			count += 1
			_ui_theme_assets_list.add_child(_make_asset_row(filename, path))
	# Mettre à jour le label de comptage
	var lbl = _ui_theme_custom_panel.get_node_or_null("AssetsCountLabel")
	if lbl:
		lbl.text = "Assets personnalisés (%d / %d)" % [count, UI_THEME_ASSETS.size()]


func _make_asset_row(filename: String, abs_path: String) -> HBoxContainer:
	var row = HBoxContainer.new()
	row.add_theme_constant_override("separation", 6)

	var preview = TextureRect.new()
	preview.custom_minimum_size = Vector2(48, 48)
	preview.expand_mode = TextureRect.EXPAND_FIT_WIDTH_PROPORTIONAL
	preview.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
	var img = Image.new()
	if img.load(abs_path) == OK:
		preview.texture = ImageTexture.create_from_image(img)
	row.add_child(preview)

	var lbl = Label.new()
	lbl.text = filename
	lbl.size_flags_horizontal = Control.SIZE_EXPAND_FILL
	row.add_child(lbl)

	var del_btn = Button.new()
	del_btn.text = "✕"
	del_btn.pressed.connect(_on_delete_ui_asset_pressed.bind(filename))
	row.add_child(del_btn)

	var replace_btn = Button.new()
	replace_btn.text = "Remplacer"
	replace_btn.pressed.connect(_on_replace_ui_asset_pressed.bind(filename))
	row.add_child(replace_btn)

	return row


func _on_delete_ui_asset_pressed(filename: String) -> void:
	if _story_base_path == "":
		return
	var path = _story_base_path + "/assets/ui/" + filename
	if FileAccess.file_exists(path):
		DirAccess.remove_absolute(path)
	_refresh_ui_theme_assets_list()


func _on_replace_ui_asset_pressed(filename: String) -> void:
	var picker = FileDialog.new()
	picker.file_mode = FileDialog.FILE_MODE_OPEN_FILE
	picker.access = FileDialog.ACCESS_FILESYSTEM
	picker.filters = ["*.png ; Images PNG"]
	picker.file_selected.connect(_on_replace_ui_asset_file_selected.bind(filename))
	add_child(picker)
	picker.popup_centered(Vector2i(800, 600))


func _on_replace_ui_asset_file_selected(path: String, filename: String) -> void:
	_import_ui_asset(path, filename)


func _on_browse_ui_assets_pressed() -> void:
	var picker = FileDialog.new()
	picker.file_mode = FileDialog.FILE_MODE_OPEN_FILES
	picker.access = FileDialog.ACCESS_FILESYSTEM
	picker.filters = ["*.png ; Images PNG"]
	picker.files_selected.connect(_on_browse_ui_assets_files_selected)
	add_child(picker)
	picker.popup_centered(Vector2i(800, 600))


func _on_browse_ui_assets_files_selected(paths: PackedStringArray) -> void:
	var ignored: Array[String] = []
	for path in paths:
		var filename = path.get_file()
		if filename in UI_THEME_ASSETS:
			_import_ui_asset(path, filename)
		else:
			ignored.append(filename)
	if ignored.size() > 0:
		_show_ignored_files_warning(ignored)
	_refresh_ui_theme_assets_list()


func _import_ui_asset(src_path: String, filename: String) -> void:
	if _story_base_path == "":
		return
	var ui_dir = _story_base_path + "/assets/ui"
	if not DirAccess.dir_exists_absolute(ui_dir):
		DirAccess.make_dir_recursive_absolute(ui_dir)
	var dest = ui_dir + "/" + filename
	DirAccess.copy_absolute(src_path, dest)
	_refresh_ui_theme_assets_list()


func _show_ignored_files_warning(ignored: Array[String]) -> void:
	var dialog = AcceptDialog.new()
	dialog.title = "Fichiers ignorés"
	dialog.dialog_text = "%d fichier(s) ignoré(s) (nom non reconnu) :\n%s" % [
		ignored.size(), "\n".join(ignored)
	]
	add_child(dialog)
	dialog.popup_centered()
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/dialogs/test_menu_config_dialog.gd
```
Attendu : PASS sur tous les tests (anciens + nouveaux)

- [ ] **Étape 5 : Commit**

```bash
git add src/ui/dialogs/menu_config_dialog.gd specs/ui/dialogs/test_menu_config_dialog.gd
git commit -m "feat(dialog): add UI theme tab with asset import/delete to menu_config_dialog"
```

---

### Task 6 : Signal + navigation_controller

**Files:**
- Modify: `src/ui/dialogs/menu_config_dialog.gd` (signal + emit)
- Modify: `src/controllers/navigation_controller.gd`
- Test: `specs/ui/dialogs/test_menu_config_dialog.gd`

- [ ] **Étape 1 : Écrire le test qui échoue**

Ajouter à `specs/ui/dialogs/test_menu_config_dialog.gd` :

```gdscript
func test_confirmed_signal_includes_ui_theme_mode() -> void:
	var story = _make_story()
	story.ui_theme_mode = "custom"
	_dialog.setup(story, "/tmp/test_story")

	var received_mode = ""
	_dialog.menu_config_confirmed.connect(
		func(_t, _st, _bg, _mu, _pf_id, _pf_en, _pa, _it,
			 _go_t, _go_st, _go_bg, _tbc_t, _tbc_st, _tbc_bg,
			 _ic, _sh, ui_mode):
			received_mode = ui_mode
	)
	_dialog._on_confirmed()
	assert_eq(received_mode, "custom", "Signal should include ui_theme_mode as 17th param")

func test_confirmed_signal_ui_theme_mode_default() -> void:
	var story = _make_story()
	story.ui_theme_mode = "default"
	_dialog.setup(story, "/tmp/test_story")

	var received_mode = "sentinel"
	_dialog.menu_config_confirmed.connect(
		func(_t, _st, _bg, _mu, _pf_id, _pf_en, _pa, _it,
			 _go_t, _go_st, _go_bg, _tbc_t, _tbc_st, _tbc_bg,
			 _ic, _sh, ui_mode):
			received_mode = ui_mode
	)
	_dialog._on_confirmed()
	assert_eq(received_mode, "default")
```

> **Note sur `navigation_controller.gd` :** La couverture de `_on_menu_config_confirmed` passe par le signal `menu_config_confirmed` — le test ci-dessus couvre la chaîne côté dialog. L'acceptation du 17e param dans navigation_controller est vérifiée par le fait que GDScript lève une erreur au signal-connect si les types ne correspondent pas.

- [ ] **Étape 2 : Lancer le test pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/dialogs/test_menu_config_dialog.gd
```
Attendu : FAIL

- [ ] **Étape 3a : Mettre à jour le signal dans `menu_config_dialog.gd`**

Remplacer la ligne 5 (déclaration du signal) :
```gdscript
signal menu_config_confirmed(menu_title: String, menu_subtitle: String, menu_background: String, menu_music: String, playfab_title_id: String, playfab_enabled: bool, patreon_url: String, itchio_url: String, game_over_title: String, game_over_subtitle: String, game_over_background: String, to_be_continued_title: String, to_be_continued_subtitle: String, to_be_continued_background: String, app_icon: String, show_title_banner: bool, ui_theme_mode: String)
```

Dans `_on_confirmed()` (ligne 645), ajouter `_ui_theme_mode` comme 17e argument :
```gdscript
func _on_confirmed() -> void:
	menu_config_confirmed.emit(
		_menu_title_edit.text, _menu_subtitle_edit.text, _menu_bg_edit.text,
		_current_menu_music, _playfab_title_id_edit.text, _playfab_enabled_check.button_pressed,
		_validate_url(_patreon_url_edit.text), _validate_url(_itchio_url_edit.text),
		_game_over_title_edit.text, _game_over_subtitle_edit.text, _game_over_bg_edit.text,
		_to_be_continued_title_edit.text, _to_be_continued_subtitle_edit.text, _to_be_continued_bg_edit.text,
		_app_icon_edit.text, _show_title_banner_check.button_pressed,
		_ui_theme_mode
	)
```

- [ ] **Étape 3b : Mettre à jour `_on_menu_config_confirmed` dans `navigation_controller.gd`**

À la ligne 474, mettre à jour la signature :
```gdscript
func _on_menu_config_confirmed(menu_title: String, menu_subtitle: String, menu_background: String, menu_music: String = "", playfab_title_id: String = "", playfab_enabled: bool = false, patreon_url: String = "", itchio_url: String = "", game_over_title: String = "", game_over_subtitle: String = "", game_over_background: String = "", to_be_continued_title: String = "", to_be_continued_subtitle: String = "", to_be_continued_background: String = "", app_icon: String = "", show_title_banner: bool = true, ui_theme_mode: String = "default") -> void:
```

Ajouter à la fin du corps de la fonction (après `_main._editor_main._story.show_title_banner = show_title_banner`) :
```gdscript
_main._editor_main._story.ui_theme_mode = ui_theme_mode
```

> **Note :** Pas de null guard nécessaire — `_main._editor_main._story` est garanti non-null à ce stade (même pattern que toutes les 16 lignes précédentes de la fonction).

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/ui/dialogs/test_menu_config_dialog.gd
```
Attendu : PASS

- [ ] **Étape 5 : Commit**

```bash
git add src/ui/dialogs/menu_config_dialog.gd src/controllers/navigation_controller.gd \
  specs/ui/dialogs/test_menu_config_dialog.gd
git commit -m "feat(signal): add ui_theme_mode as 17th param to menu_config_confirmed signal"
```

---

## Chunk 3 : Play + Jeu exporté + Export

---

### Task 7 : play_controller.gd — Thème en mode Play éditeur

**Files:**
- Modify: `src/controllers/play_controller.gd`
- Test: créer `specs/controllers/test_play_controller_ui_theme.gd`

- [ ] **Étape 1 : Écrire les tests qui échouent**

Créer `specs/controllers/test_play_controller_ui_theme.gd` :

```gdscript
extends GutTest

## Tests pour l'application du thème UI custom en mode Play éditeur.
## Les helpers reçoivent les overlays en paramètres explicites — pas de mock _main nécessaire.

const PlayControllerScript = preload("res://src/controllers/play_controller.gd")
const GameTheme = preload("res://src/ui/themes/game_theme.gd")

var _play_ctrl: Node
var _play_overlay: PanelContainer
var _choice_overlay: CenterContainer


func before_each() -> void:
	_play_overlay = PanelContainer.new()
	_choice_overlay = CenterContainer.new()
	add_child(_play_overlay)
	add_child(_choice_overlay)

	_play_ctrl = Node.new()
	_play_ctrl.set_script(PlayControllerScript)
	add_child(_play_ctrl)


func after_each() -> void:
	_play_ctrl.queue_free()
	_play_overlay.queue_free()
	_choice_overlay.queue_free()


func test_apply_play_theme_does_not_crash() -> void:
	# En headless, GameTheme.create_theme("") peut retourner un Theme ou null
	# On vérifie juste que la méthode existe et ne plante pas
	assert_true(_play_ctrl.has_method("_apply_play_ui_theme"))
	_play_ctrl._apply_play_ui_theme(_play_overlay, _choice_overlay, "")
	assert_true(_play_overlay.theme == null or _play_overlay.theme is Theme)


func test_clear_play_theme_sets_null_on_overlays() -> void:
	# D'abord appliquer un thème (peut rester null en headless — c'est OK)
	_play_ctrl._apply_play_ui_theme(_play_overlay, _choice_overlay, "")
	# Puis effacer — doit toujours mettre null
	_play_ctrl._clear_play_ui_theme(_play_overlay, _choice_overlay)
	assert_null(_play_overlay.theme, "play_overlay.theme should be null after clear")
	assert_null(_choice_overlay.theme, "choice_overlay.theme should be null after clear")
```

- [ ] **Étape 2 : Lancer le test pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/controllers/test_play_controller_ui_theme.gd
```
Attendu : FAIL (méthodes inexistantes)

- [ ] **Étape 3 : Implémenter dans `src/controllers/play_controller.gd`**

3a. Ajouter les méthodes helpers privées à la fin du fichier. Les overlays sont passés en paramètres pour éviter un couplage fort sur `_main` et faciliter les tests :
```gdscript
func _apply_play_ui_theme(play_overlay: PanelContainer, choice_overlay: CenterContainer, story_ui_path: String) -> void:
	var theme = GameTheme.create_theme(story_ui_path)
	play_overlay.theme = theme
	choice_overlay.theme = theme


func _clear_play_ui_theme(play_overlay: PanelContainer, choice_overlay: CenterContainer) -> void:
	play_overlay.theme = null
	choice_overlay.theme = null
```

3b. Dans `on_play_pressed()` (ligne 43), au **début** de la fonction (avant `_previous_play_foregrounds = []`), ajouter :
```gdscript
var _play_ui_path = ""
var _play_story = _main._editor_main.get_current_story() if _main._editor_main.has_method("get_current_story") else null
if _play_story != null and _play_story.get("ui_theme_mode") == "custom":
	_play_ui_path = _main._get_story_base_path() + "/assets/ui"
_apply_play_ui_theme(_main._play_overlay, _main._choice_overlay, _play_ui_path)
```

3c. Dans `on_top_play_pressed()` (ligne 321), même ajout au début :
```gdscript
var _play_ui_path = ""
var _play_story = _main._editor_main.get_current_story() if _main._editor_main.has_method("get_current_story") else null
if _play_story != null and _play_story.get("ui_theme_mode") == "custom":
	_play_ui_path = _main._get_story_base_path() + "/assets/ui"
_apply_play_ui_theme(_main._play_overlay, _main._choice_overlay, _play_ui_path)
```

3d. Dans `on_stop_pressed()` (ligne 118), au **début** de la fonction :
```gdscript
_clear_play_ui_theme(_main._play_overlay, _main._choice_overlay)
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/controllers/test_play_controller_ui_theme.gd
```
Attendu : PASS

- [ ] **Étape 5 : Commit**

```bash
git add src/controllers/play_controller.gd specs/controllers/test_play_controller_ui_theme.gd
git commit -m "feat(play): apply custom UI theme to play overlays in editor play mode"
```

---

### Task 8 : game.gd — Thème custom dans le jeu exporté

**Files:**
- Modify: `src/game.gd`
- Test: `specs/game/test_game_ui_builder.gd` ou créer `specs/game/test_game_ui_theme.gd`

- [ ] **Étape 1 : Écrire le test qui échoue**

Créer `specs/game/test_game_ui_theme.gd` :

```gdscript
extends GutTest

## Tests pour l'application du thème UI custom dans le jeu exporté.

const GameScript = preload("res://src/game.gd")
const StoryScript = preload("res://src/models/story.gd")
const GameTheme = preload("res://src/ui/themes/game_theme.gd")


func test_apply_game_ui_theme_method_exists() -> void:
	var game = Control.new()
	game.set_script(GameScript)
	add_child(game)
	assert_true(game.has_method("_apply_game_ui_theme"),
		"game should have _apply_game_ui_theme method")
	game.queue_free()


func test_apply_game_ui_theme_default_mode_does_not_change_theme() -> void:
	var game = Control.new()
	game.set_script(GameScript)
	add_child(game)
	var story = StoryScript.new()
	story.ui_theme_mode = "default"
	var theme_before = game.theme
	game._apply_game_ui_theme(story)
	# En mode default, le helper retourne sans toucher au thème
	assert_eq(game.theme, theme_before, "default mode should not change game.theme")
	game.queue_free()


func test_apply_game_ui_theme_custom_mode_does_not_crash() -> void:
	var game = Control.new()
	game.set_script(GameScript)
	add_child(game)
	var story = StoryScript.new()
	story.ui_theme_mode = "custom"
	# res://story/assets/ui n'existe probablement pas en test → fallback Kenney
	# Vérifier juste pas de crash et theme reste un Theme ou null
	game._apply_game_ui_theme(story)
	assert_true(game.theme == null or game.theme is Theme)
	game.queue_free()
```

- [ ] **Étape 2 : Lancer le test pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/game/test_game_ui_theme.gd
```
Attendu : FAIL

- [ ] **Étape 3 : Implémenter dans `src/game.gd`**

3a. Ajouter le helper privé dans `game.gd` (à la fin du fichier ou dans une section appropriée). Le `story` est de type `RefCounted` (classe de base des models dans ce projet) :
```gdscript
func _apply_game_ui_theme(story: RefCounted) -> void:
	if story == null or story.get("ui_theme_mode") != "custom":
		return
	var ui_path = "res://story/assets/ui"
	self.theme = GameTheme.create_theme(ui_path)
	if _main_menu and _main_menu.has_method("update_banner"):
		_main_menu.update_banner(ui_path)
	if _pause_menu and _pause_menu.has_method("apply_custom_theme"):
		_pause_menu.apply_custom_theme(ui_path)
	if _save_load_menu and _save_load_menu.has_method("apply_custom_theme"):
		_save_load_menu.apply_custom_theme(ui_path)
	if _chapter_scene_menu and _chapter_scene_menu.has_method("apply_custom_theme"):
		_chapter_scene_menu.apply_custom_theme(ui_path)
```

> **Note :** Dans le jeu exporté, les assets story sont toujours dans `res://story/`. Le chemin est donc fixe — pas de paramètre `story_base_path` nécessaire.

3b. Dans `_load_story_and_show_menu()` (ligne 277), après le chargement de la story (chercher la ligne où `story` est assigné et validé), ajouter :
```gdscript
_apply_game_ui_theme(story)
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/game/test_game_ui_theme.gd
```
Attendu : PASS

- [ ] **Étape 5 : Commit**

```bash
git add src/game.gd specs/game/test_game_ui_theme.gd
git commit -m "feat(game): apply custom UI theme after story load in exported game"
```

---

### Task 9 : export_service.gd — Bug fix + exclusion `assets/ui`

**Files:**
- Modify: `src/services/export_service.gd`
- Test: créer `specs/export/test_export_service_ui_theme.gd`

- [ ] **Étape 1 : Écrire les tests qui échouent**

Créer `specs/export/test_export_service_ui_theme.gd` :

```gdscript
extends GutTest

## Tests pour export_service — fix _copy_dir_recursive + exclusion assets/ui.

const ExportServiceScript = preload("res://src/services/export_service.gd")

var _service
var _tmp_src: String
var _tmp_dst: String


func before_each() -> void:
	_service = RefCounted.new()
	_service.set_script(ExportServiceScript)
	_tmp_src = "/tmp/test_copy_src_" + str(randi())
	_tmp_dst = "/tmp/test_copy_dst_" + str(randi())
	DirAccess.make_dir_recursive_absolute(_tmp_src)
	DirAccess.make_dir_recursive_absolute(_tmp_dst)


func after_each() -> void:
	_cleanup_dir(_tmp_src)
	_cleanup_dir(_tmp_dst)


func _cleanup_dir(path: String) -> void:
	var d = DirAccess.open(path)
	if not d:
		return
	d.list_dir_begin()
	var f = d.get_next()
	while f != "":
		var fp = path + "/" + f
		if d.current_is_dir():
			_cleanup_dir(fp)
			DirAccess.remove_absolute(fp)
		else:
			DirAccess.remove_absolute(fp)
		f = d.get_next()
	DirAccess.remove_absolute(path)


func test_copy_dir_recursive_propagates_exclude_to_subdirs() -> void:
	# Structure à 2 niveaux : _tmp_src/level1/ui/ et _tmp_src/level1/foregrounds/
	# Le bug original passe [] lors de la récursion dans level1/ → "ui" n'est plus exclu
	# Le fix propaghe ["ui"] à tous les niveaux récursifs
	DirAccess.make_dir_recursive_absolute(_tmp_src + "/level1/ui")
	DirAccess.make_dir_recursive_absolute(_tmp_src + "/level1/foregrounds")
	var f = FileAccess.open(_tmp_src + "/level1/ui/button_brown.png", FileAccess.WRITE)
	f.store_string("fake png")
	f.close()
	f = FileAccess.open(_tmp_src + "/level1/foregrounds/hero.png", FileAccess.WRITE)
	f.store_string("fake png")
	f.close()

	_service._copy_dir_recursive(_tmp_src, _tmp_dst, ["ui"])

	# "ui" doit être exclu à tous les niveaux
	assert_false(DirAccess.dir_exists_absolute(_tmp_dst + "/level1/ui"),
		"level1/ui should be excluded by propagated exclude list")
	assert_true(DirAccess.dir_exists_absolute(_tmp_dst + "/level1/foregrounds"),
		"level1/foregrounds should be copied")


func test_copy_dir_recursive_without_exclude_copies_everything() -> void:
	DirAccess.make_dir_recursive_absolute(_tmp_src + "/assets/ui")
	var f = FileAccess.open(_tmp_src + "/assets/ui/button_brown.png", FileAccess.WRITE)
	f.store_string("fake png")
	f.close()

	_service._copy_dir_recursive(_tmp_src, _tmp_dst, [])

	assert_true(DirAccess.dir_exists_absolute(_tmp_dst + "/assets/ui"),
		"assets/ui should be copied when no exclusion")
	assert_true(FileAccess.file_exists(_tmp_dst + "/assets/ui/button_brown.png"))
```

- [ ] **Étape 2 : Lancer les tests pour vérifier l'échec**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/export/test_export_service_ui_theme.gd
```
Attendu : FAIL sur `test_copy_dir_recursive_propagates_exclude_to_subdirs`

- [ ] **Étape 3a : Corriger le bug dans `_copy_dir_recursive` (ligne 350)**

Dans `src/services/export_service.gd`, trouver la ligne :
```gdscript
_copy_dir_recursive(from_path, to_path, [])
```
La remplacer par :
```gdscript
_copy_dir_recursive(from_path, to_path, exclude)
```

- [ ] **Étape 3b : Ajouter l'exclusion conditionnelle de `assets/ui`**

Dans `export_story()` (ligne 67), trouver :
```gdscript
_copy_dir_recursive(abs_story_dir, abs_temp_story, ["artbook"])
```
Remplacer par :
```gdscript
var story_exclude = ["artbook"]
if story.get("ui_theme_mode") != "custom":
	story_exclude.append("ui")
_copy_dir_recursive(abs_story_dir, abs_temp_story, story_exclude)
```

- [ ] **Étape 4 : Lancer les tests**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 30 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://specs/export/test_export_service_ui_theme.gd
```
Attendu : PASS

- [ ] **Étape 5 : Lancer la suite complète**

```bash
GODOT=${GODOT_PATH:-/Applications/Godot-4.6.1.app/Contents/MacOS/Godot}
timeout 120 $GODOT --headless --path . -s addons/gut/gut_cmdln.gd
```
Attendu : tous les tests passent (y compris les anciens)

- [ ] **Étape 6 : Commit final**

```bash
git add src/services/export_service.gd specs/export/test_export_service_ui_theme.gd
git commit -m "fix(export): propagate exclude list in _copy_dir_recursive; exclude assets/ui in default theme mode"
```

---

## Récapitulatif des fichiers modifiés

| Fichier | Type | Changement |
|---|---|---|
| `src/models/story.gd` | Modify | Champ `ui_theme_mode`, `to_dict()`, `from_dict()` |
| `src/ui/themes/game_theme.gd` | Modify | `_resolve_asset()`, signatures avec `story_ui_path` |
| `src/ui/menu/main_menu.gd` | Modify | `_banner_texture_rect`, `update_banner()` |
| `src/ui/menu/pause_menu.gd` | Modify | `apply_custom_theme()` |
| `src/ui/menu/save_load_menu.gd` | Modify | `apply_custom_theme()` |
| `src/ui/menu/chapter_scene_menu.gd` | Modify | `apply_custom_theme()` |
| `src/ui/dialogs/menu_config_dialog.gd` | Modify | Onglet "Thème UI", signal 17 params, `_on_confirmed` |
| `src/controllers/navigation_controller.gd` | Modify | `_on_menu_config_confirmed` + 17e param |
| `src/controllers/play_controller.gd` | Modify | `_apply_play_ui_theme()`, `_clear_play_ui_theme()` |
| `src/game.gd` | Modify | `_apply_game_ui_theme()`, appel dans `_load_story_and_show_menu()` |
| `src/services/export_service.gd` | Modify | Fix `_copy_dir_recursive`, exclusion `ui` |
| `specs/models/test_story.gd` | Modify | 5 nouveaux tests |
| `specs/ui/themes/test_game_theme.gd` | Modify | 7 nouveaux tests |
| `specs/ui/menu/test_main_menu.gd` | Modify | 2 nouveaux tests |
| `specs/ui/menu/test_pause_menu.gd` | Modify | 2 nouveaux tests |
| `specs/ui/menu/test_save_load_menu.gd` | Modify | 2 nouveaux tests |
| `specs/ui/menu/test_chapter_scene_menu.gd` | Modify | 2 nouveaux tests |
| `specs/ui/dialogs/test_menu_config_dialog.gd` | Modify | 4 nouveaux tests |
| `specs/controllers/test_play_controller_ui_theme.gd` | Create | 2 tests |
| `specs/game/test_game_ui_theme.gd` | Create | 3 tests |
| `specs/export/test_export_service_ui_theme.gd` | Create | 2 tests |
